# Correo público

**Bandeja actual del medio:** `mascotasargentinas@duck.com` (Duck Address). Es la dirección en `/contacto`, el footer, `/legal` y el JSON-LD. El formulario de contacto y el newsletter siguen yendo a Supabase; este correo es para responder.

Workspace en `mascotasargentinas.com.ar` queda como opción cuando quieras cara de marca en el dominio. Hasta entonces no hace falta MX ni alias `hola@` / `prensa@` / `comercial@`.

El instructivo de Google (MX, SPF, DKIM, alias) se puede retomar más adelante; no es el canal que el sitio publica hoy.
